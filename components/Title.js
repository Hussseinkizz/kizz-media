const Title = ({ text }) => {
  return (
    <h1 className="font-heading css-tricks-gradient-on-border-bottom width-content center-with-margin css-tricks-gradient-on-text">
      {text}
    </h1>
  );
};

export default Title;
